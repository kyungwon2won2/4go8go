package com.example.demo.common.oauth.service;

/**
 * 이 클래스는 더 이상 사용되지 않습니다.
 * CustomOAuth2UserService에서 OAuth2UserService 인터페이스를 직접 구현하므로 
 * 이 구현체는 필요하지 않게 되었습니다.
 */
public class OAuth2UserServiceImpl {
    // 사용하지 않는 클래스
}