<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.UserMapper">

    <resultMap type="Users" id="userMap">
        <id property="userId" column="userId" />
        <result property="userPw" column="userPw" />
        <result property="name" column="name" />
        <result property="email" column="email" />
        <result property="birthday" column="birthday" />
        <result property="enabled" column="enabled" />
        <result property="regDate" column="regDate" />
        <result property="updDate" column="updDate" />
        <collection property="authList" resultMap="authMap" />
    </resultMap>

    <resultMap type="UserAuth" id="authMap">
        <result property="userId" column="user_id" /> <!-- user_id로 수정 -->
        <result property="auth" column="auth" />
    </resultMap>

    <!-- 로그인 처리 -->
    <select id="login" resultMap="userMap">
        SELECT u.userId,
        u.userPw,
        u.name,
        u.email,
        u.birthday,
        u.enabled,
        u.regDate,
        u.updDate,
        auth.auth
        FROM user u
        JOIN user_auth auth ON u.userId = auth.user_id  <!-- userId -> user_id로 수정 -->
        WHERE u.userId = #{userId}
    </select>

    <!-- 오늘 생일인 사용자 목록 조회 -->
    <select id="findUsersByBirthdayToday" resultMap="userMap">
        SELECT u.userId,
        u.userPw,
        u.name,
        u.email,
        u.birthday,
        u.enabled,
        u.regDate,
        u.updDate,
        auth.auth
        FROM user u
        JOIN user_auth auth ON u.userId = auth.user_id
        WHERE MONTH(u.birthday) = MONTH(CURRENT_DATE)
        AND DAY(u.birthday) = DAY(CURRENT_DATE)
        AND u.enabled = 1
    </select>

</mapper>
